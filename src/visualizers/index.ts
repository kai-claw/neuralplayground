/**
 * Visualizers barrel export.
 *
 * Pure computation functions for visualization layout and particle generation.
 * No DOM or React dependency.
 */

export {
  computeNodePositions,
  generateParticles,
  getLayerSizes,
  type NodePos,
  type Particle,
} from './networkLayout';
